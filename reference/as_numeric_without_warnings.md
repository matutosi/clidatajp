# Wrapper function to convert into numeric without warnings

Wrapper function to convert into numeric without warnings

## Usage

``` r
as_numeric_without_warnings(x)
```

## Arguments

- x:

  A string.

## Value

A numeric or NA.
